package com.corestack.backend.common.exception;

public enum ErrorCode {
    VALIDATION_ERROR,
    RESOURCE_NOT_FOUND,
    UNAUTHORIZED,
    FORBIDDEN,
    CONFLICT,
    INTERNAL_ERROR,
    INVALID_CREDENTIALS,
    SPLIT_TOTAL_MISMATCH
}
